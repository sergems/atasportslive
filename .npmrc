auto-install-peers=false
strict-peer-dependencies=false

# Allow native build scripts for these packages (required by pnpm 11 in CI/Docker)
# pnpm 11 requires comma-separated list, not array syntax
onlyBuiltDependencies=bcrypt,esbuild,@swc/core,msw,unrs-resolver
